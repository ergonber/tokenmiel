/**
 * Drizzle schema barrel — M1 stub.
 *
 * Real schema tables (audit_log, holdings, lots, identity, etc.) are defined in M1.
 * Exporting an empty object here so the package resolves and type-check stays green.
 */

export const schema = {} as const;
