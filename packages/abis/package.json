{
  "name": "@tokenization/abis",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "generate": "bun scripts/generate.ts",
    "test": "vitest run",
    "type-check": "tsc --noEmit",
    "lint": "biome check ./src"
  },
  "dependencies": {
    "abitype": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@biomejs/biome": "^1.9.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.0.0"
  }
}
